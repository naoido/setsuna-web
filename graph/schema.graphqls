# GraphQLスキーマの例
#
# https://gqlgen.com/getting-started/

# Todo型を定義しています。TodoはID、テキスト、完了状態、ユーザーを持ちます。
type Todo {
  id: ID!       # Todoの一意な識別子
  text: String! # Todoの内容
  done: Boolean! # Todoが完了しているかどうか
  user: User!   # Todoを作成したユーザー
}

# User型を定義しています。UserはIDと名前を持ちます。
type User {
  id: ID!   # ユーザーの一意な識別子
  name: String! # ユーザーの名前
}

# Query型を定義しています。クエリはTodoのリストを返します。
type Query {
  todos: [Todo!]! # 全てのTodoを取得するクエリ
}

# Mutation型��定義しています。ミューテーションは新しいTodoを作成します。
type Mutation {
  createTodo(input: NewTodo!): Todo! # 新しいTodoを作成するミューテーション
}

# サブスクリプション型を定義しています。新しいTodoが作成されたときに通知します。
type Subscription {
  todoAdded: Todo! # 新しいTodoが作成されたときに通知するサブスクリプション
}

# 新しいTodoを作成するための入力型を定義しています。
input NewTodo {
  text: String! # 新しいTodoの内容
  userId: String! # 新しいTodoを作成するユーザーのID
}
